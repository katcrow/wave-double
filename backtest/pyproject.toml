[project]
name = "backtest"
version = "0.1.0"
description = "Adopted strategy/metric kernel — logic unchanged"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas==3.0.5",
    "numpy==2.5.2",
    "pyarrow==25.0.1",
]
