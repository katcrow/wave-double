[project]
name = "batch"
version = "0.1.0"
description = "Python CLI batch orchestrator"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx==0.28.1",
    "tzdata==2025.2; sys_platform == 'win32'",
]
