// Never: `/tracking` 콘텐츠(성과 검증)는 이번 스토리에서 자리표시자 헤딩만 둔다.
export default function TrackingPage() {
  return (
    <section aria-labelledby="tracking-heading">
      <h1 id="tracking-heading">성과 검증</h1>
      <p>성과 검증 준비 중입니다.</p>
    </section>
  );
}
