---
epic: 5
date: 09-14-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 5 회고 — 실전 성과 검증 화면 & 모집단 편향

## Epic 요약

- **범위:** 5개 에픽 중 최대 규모(14개 스토리), 3개 영역 — 편향 스키마/계산(5.1-5.4) → 지표 view(5.5-5.10) → 화면(5.11-5.14).
- **Diff 범위:** `d236901^..9c64b73` (24개 커밋, merge 0건). 모든 커밋이 5-1~5-14 스토리에 귀속되며, 스토리 귀속 없는 커밋 1건(`b45770d fix: harden outcome metric fixture contracts`)은 5-14 작업 직후의 픽스처 보강.
- **미완료 스토리:** 없음 — `sprint_status.py detect-epic --epic 5` 결과 `pending_stories: []`, 14개 스토리 전부 `done`.
- **증거 인벤토리:**
  - 확보: epic spec(`_bmad-output/planning-artifacts/epics.md` Epic 5 절, 14개 스토리 AC 전체), epic context(`epic-5-context.md`), 스토리별 spec 14개, git evidence(커밋/파일 변경량), `deferred-work.md`의 epic-5 관련 기존 기록, epic-4 회고 및 sprint-status 후속 조치 상태.
  - 미확보/미실행: 세션 로그(대화 기록) — 접근 불가하여 프로세스 교훈 분석은 코드·리뷰 커밋 메시지에서만 도출. Playwright를 통한 실제 화면 조작 검증은 이번 패스에서 실행하지 않음(아래 "행동 검증" 참조).
- **실행 모드:** 사용자가 "스스로 알아서 진행해요"로 자율 진행을 요청 — headless 모드로 처리, 가정 사항은 아래 Assumptions에 기록.

## 발견 사항 (Findings)

각 항목은 출처(파일:라인 또는 커밋)를 명시한다. 배치는 아래 각주에 fix-now / defer / accept-as-is로 표기.

### A. 중복(Duplication)

1. **[fix-now] SQL 픽스처 파서 4중 복제** — `tools/check_outcome_win_rate_pf_parity.py:45-186`, `check_outcome_win_rate_pf_gate_parity.py:56-235`, `check_outcome_win_rate_pf_ci_parity.py`, `check_outcome_win_rate_pf_source_parity.py:46-121`가 `_extract_value_tuples`/`_split_preserving_quotes`/`_raw_to_python`/`canonical_4dp`/`_row_key` 등을 거의 동일하게 복사-붙여넣기했다. 이스케이프된 따옴표(`''`)나 인용 문자열 내 괄호를 파싱하지 못하는 결함이 4개 사본 모두에 그대로 상속되어 있다 — `deferred-work.md`(5-6, 5-7, 5-8 리뷰 후속)에서 스토리마다 반복 지적됐지만 한 번도 통합되지 않았다. 현재 fixture 데이터에는 해당 문자가 없어 미발현이지만, 발현 시 4곳을 동시에 고쳐야 한다.
2. **[fix-now] 5.14 `get_outcome_metric_comparison`이 5.7~5.10 로직을 재구현** — `infra/supabase/migrations/202609141200_create_source_filtered_tracking_read_models.sql:7-177`가 30건 표본 게이트·Wilson 95% CI(z=1.959963985)·±10%p/±25% 임계값 판정을 기존 `_gated`/`_ci_gated`/`_threshold_gated` view 체인을 조합하는 대신 인라인으로 다시 작성했다. 이 RPC에는 대응하는 parity checker가 없고, `tests/sql/test_get_source_filtered_tracking.sql`의 fixture는 4행뿐이라 `sample_gate_passed`가 항상 false — 즉 재구현된 CI/임계값 산식이 실제 숫자로 한 번도 실행 검증되지 않았다. 소비처: `apps/web/app/tracking/page.tsx:102-105` → `MetricComparisonPanel.tsx`. 중복이자 검증 공백이 겹친 항목.
3. **[defer] Primary source 결정 로직 3중 복제** — weight 내림차순 → `t1859>t1852>t1856` 우선순위 규칙이 5.6 migration, 5.14 migration, `apps/batch/bias_metrics.py`의 `resolve_primary_source()`에 각각 독립 구현되어 있다.
4. **[process lesson] Parity-checker 컨벤션 분기** — 기존 epic-3/6의 `check_production_parity.py`, `check_outcome_rebuild_fixture_drift.py`는 더 단순한 자체 파싱을 쓰는데, epic 5는 story 5.5에서 새 SQL-fixture 파싱 컨벤션을 도입해 5회 그대로 복제했다. 기존 컨벤션과 통합하지 않고 새 갈래를 만든 것 자체가 재발 방지 대상.

### B. 정합성/일관성

5. **[fix-now] Source 필터가 outcome row 목록에 전파되지 않음** — `get_outcome_tracking_rows`에 source 파라미터가 연결되지 않아, 원천 필터 적용 시 Metric/Bias 패널은 필터링된 값을 보여주는데 Outcome row 목록은 계속 전체 원천을 보여줄 수 있다. 5.14의 AC("선택한 원천 기준으로 승률·PF·편향 지표가 분리되어 표시된다")와 UX 원칙(화면 간 필터 일관성)에 직접 저촉.
6. **[fix-now] Outcome row double-count 위험** — `infra/supabase/migrations/202609101200_create_outcome_win_rate_pf_by_strategy_source.sql` 인근 lateral join과 `202609141200_create_source_filtered_tracking_read_models.sql` 인근 lateral join에 `LIMIT 1`(또는 동등한 유일성 보장)이 누락되어, 한 candidate가 `(attempt_run_id, ticker)` 기준으로 2행 이상 가지면 승률·PF가 부풀려질 수 있다. 이 화면이 V2 진행 여부의 유일한 판단 근거라는 점에서 무겁게 볼 결함.
7. **[fix-now] `OutcomeTrackingPanel.tsx` 필터 초기화 시 URL 파라미터 미삭제** — 값이 빈 선택이 되어도 URL 쿼리 파라미터를 지우지 않아, 형제 패널(Metric/Bias)과 동작이 다르다.

### C. Spec 반영 재확인 (Spec-to-implementation reconciliation)

8. **[clean]** CI 우선 판정 규칙 — `_ci_gated`(5.8)는 `_gated`(5.7)를 select해 표본 게이트를 재정의하지 않고, `_threshold_gated`(5.9)는 부가적일 뿐 CI 판정을 대체하지 않는다. 마이그레이션 직접 확인.
9. **[clean]** append-only + canonical "최신 이벤트 선택" — `202609091700_create_bias_events.sql`, `202609100200_bias_close_stage.sql`에서 확인.
10. **[clean]** bias stage 실패 격리 — `apps/batch/scheduler.py:350-357`가 `run_bias_stage`를 `except Exception`으로 감싸 옵션 stage 실패가 candidates/tags/outcome 발행 성공을 막지 않음을 명시적 주석과 함께 구현.
11. **[미확인 — 범위 축소]** TIMEOUT 왜곡 고지 값(N=30 기준 A±0/B PF+0.0125/C±0)이 `tests/sql/test_outcome_win_rate_pf_threshold_gated.sql`에 정확히 그 상수로 단언되어 있는지는 이번 패스에서 직접 열어 재확인하지 못했다 — "확인 후 이상 없음"이 아니라 "미확인"으로 기록한다.

### D. 기타 (경증)

12. **[defer]** `universe_signal.py:4159-4162` — NaN이 진리값 검사에서 truthy로 처리되어 시그널 건수를 부풀릴 수 있음.
13. **[defer]** `MetricComparisonPanel.tsx`의 ALL_LOSS 케이스가 ALL_WIN과 대칭인 설명 문구를 갖지 않음.
14. **[accept-as-is]** 5.14 migration의 중복 `revoke select` 재선언 — 무해하지만 혼란을 줄 수 있어 주석 권장.
15. **[carry-forward, 여전히 open]** `deferred-work.md`(5-6 리뷰)에 기록된 "4개 gated view(`candidate_outcome_win_rate_pf`, `_by_strategy_gated`, `_ci_gated`, `_by_strategy_source`)에 대해 anon/authenticated의 실제 SELECT 차단을 검증하는 자동 SQL 테스트 부재" — 스토리 5-6/5-7/5-8 리뷰에서 3차례 반복 지적됐으나 아직도 미해결. 회고 시점에 에픽 단위 action item으로 승격.

### 12번(god-class 점검)

- `apps/batch/bias_metrics.py`(554줄): dataclass 4개 + 함수 13개로 건강하게 분해되어 있다. **god-class 아님**.
- `tools/check_outcome_win_rate_pf_source_parity.py`(711줄): 크기 자체는 JSON/SQL/pgTAP 3중 검증이라는 성격상 불가피하다. 크기는 accept-as-is, 다만 그 안의 파서 코드는 위 finding #1의 중복 대상.

## 행동 검증 (Behavior check)

Playwright 등 브라우저 조작으로 `/tracking` 라우트를 이번 패스에서 직접 실행/관찰하지 않았다. `e2e/tracking.spec.ts` + `e2e/mock-supabase-server.mjs`가 diff에 포함되어 있어 자동화 커버리지는 존재하나, 이는 "실행해 확인함"이 아니라 "코드 상 존재를 확인함"이다 — 실제 조작 검증은 미실시로 기록한다.

## 이전 회고 후속 조치 (Previous-retro follow-through)

`sprint-status.yaml`의 `action_items` 중 이전 에픽 소속이면서 아직 `done`이 아닌 항목:

| id / (epic, action) | 상태 | 근거 |
|---|---|---|
| `epic-1-retro-item-2` (epic 1, 운영 Supabase migration 순차 적용) | in-progress (기존) | epic-5 diff/증거에서 이 항목에 대한 새 증거를 찾지 못함 — "증거 없음", 상태 변경 제안 없음 |
| `epic-3-retro-item-18` (epic 3, SUSPENDED GitHub Issue·14일 escalation·자동 DELISTED 감지) | open (기존) | 동일 — epic 5 작업 범위와 무관, 새 증거 없음 |
| `story-6-8-item-1` (epic 6, 전략 E OOS 20건 누적 재평가) | open (기존) | 동일 — 무관, 새 증거 없음 |
| `story-7-7-item-1` (epic 7, 전략 F OOS 200건 누적 재평가) | open (기존) | 동일 — 무관, 새 증거 없음 |

headless 실행이므로 `--set-action-status`는 사용하지 않았고, 위 표는 제안이 아니라 "확인했으나 변경 근거 없음"의 기록이다.

## Action Items

| # | 내용 | 유형 | Owner | 근거 |
|---|---|---|---|---|
| 1 | `check_outcome_win_rate_pf*_parity.py` 4개 사본의 공통 파서(`_extract_value_tuples`/`_split_preserving_quotes`/`_raw_to_python`/`canonical_4dp`/`_row_key`)를 공유 모듈로 추출하고 이스케이프 따옴표/괄호 파싱 결함을 한 번에 수정 | 리메디에이션 | 개발 | Finding A1, deferred-work.md(5-6/5-7/5-8 리뷰) |
| 2 | `get_outcome_metric_comparison`(5.14)이 5.7~5.10 view 체인을 조합하도록 리팩터링하거나 최소한 parity checker를 추가하고, 30건 게이트를 실제로 통과하는 fixture로 CI/임계값 산식을 검증 | 리메디에이션 | 개발 | Finding A2 |
| 3 | `202609101200`/`202609141200` 인근 lateral join에 `LIMIT 1`(또는 유일성 보장)을 추가해 outcome 이중 집계 가능성 제거 | 리메디에이션 | 개발/데이터 | Finding B6 |
| 4 | `get_outcome_tracking_rows`에 source 필터 파라미터 연결 | 리메디에이션 | 개발/프론트엔드 | Finding B5 |
| 5 | `OutcomeTrackingPanel.tsx` 필터 초기화 시 URL 파라미터 삭제 로직을 형제 패널과 동일하게 수정 | 리메디에이션 | 프론트엔드 | Finding B7 |
| 6 | 4개 gated view의 anon/authenticated SELECT 차단을 검증하는 자동 SQL 테스트 추가 (5-6 리뷰부터 3회 반복 지적, 미해결) | 리메디에이션 | 개발/운영 | Finding D15, deferred-work.md |
| 7 | Primary source 결정 로직(weight desc → t1859>t1852>t1856)을 단일 소스로 추출하고 언어 간(SQL/Python) parity 테스트 추가 | 리메디에이션(defer) | 개발 | Finding A3 |
| 8 | 5.8 Wilson CI가 5.7의 반올림된 `win_rate`를 그대로 중심값으로 쓰는지, 아니면 원시 `wins`/`total_settled`로 재계산하는지 경계값 fixture로 확인 | 검증(defer) | 개발 | Finding C(경계 확인) |
| 9 | `universe_signal.py`의 NaN truthy 시그널 판정에 명시적 finite 체크 추가 | 리메디에이션(defer) | 개발 | Finding D12 |
| 10 | `MetricComparisonPanel.tsx` ALL_LOSS 케이스에 ALL_WIN과 대칭인 설명 문구 추가 | 리메디에이션(defer) | 프론트엔드 | Finding D13 |
| 11 | (프로세스 교훈) parity-checker 스캐폴딩은 2번째 사본이 아니라 그 전에 공유 모듈로 뽑아내고, 기존 epic-3 fixture-parity 컨벤션과 통합 검토할 것 | 프로세스 개선 | 개발 프로세스 | Finding A4 |
| 12 | TIMEOUT 왜곡 고지 상수(A±0/B+0.0125/C±0)가 `test_outcome_win_rate_pf_threshold_gated.sql`에 정확히 단언되는지 별도로 확인 | 검증(defer) | 개발 | Finding C11(미확인) |

## Acceptance Verdict

**accepted-with-open-items** (기준: **declared** — `epics.md` Epic 5 절 14개 스토리 AC + `epic-5-context.md`의 성공 판정 기준)

- 미완료 스토리 없음 (`pending_stories: []`) — accepted 계열 판정의 전제 충족.
- 승률/PF 핵심 산식, 표본 게이트, CI 우선순위, 이탈 임계값, 컷오프 편향 고지, append-only 편향 스키마, source 권위 규칙, bias stage 격리 등 선언된 핵심 요구사항은 코드 상 구현을 직접 확인했다(위 "clean" 표기 항목).
- 다만 다음 열린 발견이 남아 있어 무조건 accepted로 판정하지 않는다: (a) 5.14의 CI/임계값 로직이 실제 30건 이상 fixture로 한 번도 검증되지 않은 점, (b) 이중 집계 가능성이 있는 lateral join, (c) source 필터가 화면 간 불일치를 유발할 수 있는 점. 이 셋은 "V2 진행 여부의 유일한 근거"라는 에픽 목표에 직접 걸리는 정확성 리스크이므로 열린 상태로 명시적으로 추적한다.
- 4개 view의 RLS SELECT 차단 자동 테스트 부재는 3회 연속 반복 지적된 채 미해결로 남아, 프로세스 실패(리뷰가 잡아낸 것을 다음 스토리가 흡수하지 않음)로도 기록한다.

## Open Questions

- TIMEOUT 왜곡 상수 단언 여부(Finding C11)는 사람이 직접 열어 확인하거나 다음 세션에서 재확인이 필요하다.
- 5.8의 CI 중심값이 반올림된 값인지 원시값인지는 경계 fixture 없이는 실제 영향(승/패 판정 뒤집힘)을 단정할 수 없다.
- `/tracking` 화면의 실제 조작 검증(Playwright)은 수행되지 않았다 — 사람이 필요하다고 판단하면 별도로 실행 요청.

## Assumptions (headless 실행 기록)

- 대상 에픽: 사용자 지시("epic-5")를 그대로 사용, 별도 auto-detect 불필요.
- `detect-epic --epic 5` 결과: `story_count: 14`, `pending_stories: []` — 미완료 스토리 없음을 확인. 사람 확인 없이 진행.
- headless이므로 팀 토론(Phase 3)은 실행하지 않음(옵트인이며 사용자가 요청하지 않음).
- 각 finding의 fix-now/defer/accept-as-is 배치는 이 회고 세션이 자율적으로 판단한 것이며, 사람 확인을 거치지 않았다.
- 판정(verdict)은 사람 개입 없이 machine 판정을 그대로 채택했다(accepted-with-open-items) — 근거는 위 Acceptance Verdict 절.
- 이전 회고 미해결 action item(epic-1/3/6/7)에 대해 상태 변경을 제안하지 않았다(headless 규칙).
