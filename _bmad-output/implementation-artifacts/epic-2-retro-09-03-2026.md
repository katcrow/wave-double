---
epic: 2
date: 09-03-2026 10:00
verdict: rejected
criteria: declared
headless: false
---

# 에픽 2 회고 — 전략 태깅 & 오늘의 후보 노출

## Epic summary

- **범위:** FR3, FR3a, FR3b — 일봉 캐시, 공유 전략 API, 골든 회귀 게이트, 후보 태깅·수급 스키마, 오늘의 후보 카드와 장중 시그널 유지·소멸 표시.
- **완료 상태:** `detect-epic --epic 2` 결과는 8개 스토리 모두 `done`, `pending_stories: []`였다. 근거: `sprint-status.yaml:53-62`.
- **검토 범위:** `a222cbfcdd5de417fed3cb4855876e72fe8839c1^..a685fae92a22bd26e7c26bfd8541b06f7aec5af2` — 최초 2.1 구현 커밋의 부모부터 2.8 완료 동기화까지, 19개 non-merge 커밋. `git diff --check` 통과.
- **확인한 산출물:** `epics.md`의 Epic 2 명세, `spec-2-1`~`spec-2-8`, `epic-2-context.md`, 전체 diff/커밋, sprint status, Epic 1 회고, Python·웹 테스트·CI workflow, Supabase migration/SQL fixture.
- **누락된 증거:** 스토리별 세션 로그는 저장소에 없다. Supabase CLI/MCP 연결이 현재 도구에 없어 운영 프로젝트의 migration 적용·RPC·SQL fixture 실행은 확인하지 못했다. 인증 세션을 발급하는 Playwright MCP도 현재 사용할 수 없어 보호 라우트의 실제 카드 렌더링은 검증하지 못했다.

## Findings

### Aggregate views

#### F1 — 공통 배치 회귀망이 현재 깨져 있다

- **근거:** `run_tags_stage()`가 `tags_repo.sync_vanished(run_id_str)`를 필수 호출한다(`apps/batch/tags_stage.py:201-207`). 기존 scheduler fake는 이 메서드를 구현하지 않는다(`tests/batch/test_scheduler.py:27-39`). 전체 Python 실행 결과 `234 passed, 7 failed`; 실패한 성공·부분성공·수동 dispatch·calendar fallback 시나리오가 모두 `partial`을 반환했다(`tests/batch/test_scheduler.py:193-621`).
- **영향:** Epic 1 scheduler 회귀 테스트와 Epic 2 변경의 repository protocol이 불일치해 CI가 실패한다.
- **처리:** **fix now** — 공통 fake를 새 protocol에 맞추고, sync 실패 시 의도된 partial과 하위 호환 경로를 별도 테스트한다.

#### F2 — 운영 데이터 조정 방식과 골든 게이트의 권위가 분리돼 있다

- **근거:** 골든 gate는 `backtest/data/raw/*.parquet`에서 만든 yfinance 파생 fixture만 사용한다(`spec-2-4-골든-픽스처-회귀-테스트.md:14-19, 155`). 명세 자체도 LS `sujung=Y` 동등성 단발성 검증 전에는 Jaccard 통과가 운영 재현 증명이 아니라고 기록한다(`spec-2-4-골든-픽스처-회귀-테스트.md:50-52`).
- **영향:** A/B/C 로직은 통과해도 운영 가격 조정 기준에서 신호가 달라지는 위험이 남는다.
- **처리:** **defer** — LS fixture 또는 별도 adjustment-equivalence 검증을 Epic 3 진입 gate로 명시한다.

#### F3 — tags stage의 장시간 실행은 lease 만료에 취약하다

- **근거:** tags stage가 후보별 OHLCV 로딩·계산·저장을 한 번의 실행 흐름으로 수행한다(`apps/batch/tags_stage.py:108-176`). Story 2.5가 LS TR별 1초 제한에서 `lease_seconds=300` 만료 가능성과 heartbeat 부재를 명시적으로 deferred로 남겼다(`spec-2-5-후보-태깅-stage-저장.md:25-35`).
- **영향:** 신규 후보가 많으면 lease가 만료되어 정상 계산이 완료돼도 stage write가 실패할 수 있다.
- **처리:** **defer** — 장시간 stage 공통 heartbeat/lease 연장 정책을 다음 배치 hardening 작업으로 승격한다.

#### F4 — 태그 일괄 저장 실패 시 계산 결과가 전부 폐기된다

- **근거:** `tags_repo.upsert_tags(all_tags)`가 전체 태그를 한 번에 저장하고 실패 시 `TAGS_PERSIST_FAILED`로 종료한다(`apps/batch/tags_stage.py:169-190`). Story 2.5는 한 행의 FK 등 오류가 다른 종목의 태그까지 폐기하는 위험을 deferred로 기록한다(`spec-2-5-후보-태깅-stage-저장.md:13-24`).
- **영향:** 부분 계산 성공이 저장 단계에서 조용히 전체 실패로 축소되고 재처리 단위가 커진다.
- **처리:** **defer** — 종목 단위 저장/격리 또는 원자성 정책을 명시하는 별도 설계를 만든다.

#### F5 — 새 카드·소멸 표시의 보호 라우트 런타임 검증이 없다

- **근거:** Story 2.7과 2.8은 인증 세션을 발급하는 e2e fixture가 없어 카드·장중 라벨 통합 렌더가 자동 검증되지 않는다고 기록한다(`spec-2-7-오늘의-후보-카드-ui.md:13-22`, `spec-2-8-장중-시그널-유지-소멸-표시.md:13-25`). 현재 웹 단위 테스트는 순수 view-model/label 함수만 검증한다(`apps/web/lib/candidate-cards.test.ts`, `apps/web/lib/dashboard-types.test.ts`, `apps/web/lib/disappeared-candidates.test.ts`).
- **영향:** RPC 응답 shape, 인증 세션, 서버 컴포넌트 조건부 렌더가 실제 화면에서 깨져도 단위 테스트가 통과할 수 있다.
- **처리:** **defer** — 안전한 인증 테스트 계정 또는 staging-equivalent fixture를 확보한 뒤 Playwright MCP smoke를 추가한다.

#### F6 — 운영 Supabase 적용 및 실제 SQL 계약 검증의 증거가 이 회고에 없다

- **근거:** Epic 2 SQL fixture와 migration은 저장소에 존재하지만, 이 실행 환경에서는 `supabase` CLI가 인식되지 않았고 Supabase MCP도 연결되지 않았다. 따라서 `tests/sql/test_daily_ohlcv.sql`, `test_candidate_tags.sql`, `test_supply_3day.sql`, `test_get_today_candidate_cards.sql`, `test_sync_vanished_tags.sql`, `test_get_today_disappeared_candidates.sql`의 실제 DB 실행 결과를 확인하지 못했다. 각 fixture는 실행 후 검증을 요구한다(예: `tests/sql/test_get_today_disappeared_candidates.sql:1-10`).
- **영향:** migration 문법·권한·RPC 반환 계약이 실제 운영 프로젝트에서 맞는지 판단할 수 없다.
- **처리:** **fix now** — 운영 프로젝트 `qqhjeumlecaudsiqhhdu`에 순서대로 적용하고 rollback fixture 결과와 function grant/RLS 결과를 증거로 남긴다.

#### F7 — SQL CI 단계의 표시명이 실제 범위를 설명하지 않는다

- **근거:** job은 Epic 2 fixture까지 모두 실행한다(`.github/workflows/test.yml:92-110`)지만 step 이름은 `Run Epic 1 SQL fixtures`다(`.github/workflows/test.yml:104`). Story 2.6도 이를 기존 label 오류로 기록한다(`spec-2-6-supply_3day-스키마.md:14-24`).
- **영향:** CI 로그를 읽는 사람이 Epic 2 SQL 계약이 실행되지 않았다고 오해하거나 새 fixture 추가 범위를 놓칠 수 있다.
- **처리:** **accept as-is** — 기능 결함은 아니며 다음 workflow 정리 때 이름만 `Run SQL fixtures`로 정리한다.

#### F8 — `get_today_disappeared_candidates` 오류가 기존 카드 상태와 비대칭적으로 처리된다

- **근거:** 카드 RPC 오류는 `candidateCardsFetchFailed`를 세워 상태 표시 대상으로 삼지만(`apps/web/app/page.tsx:39-50`), disappeared RPC 오류는 console에만 기록하고 조용히 생략한다(`apps/web/app/page.tsx:53-70`).
- **영향:** 모집단 이탈/수집 실패 안내가 조회 실패로 사라져 사용자가 “이탈 없음”으로 오해할 수 있다.
- **처리:** **defer** — 신규 RPC 실패도 데이터 신뢰 상태에 반영할지 UI 계약을 결정하고 실패 banner 또는 명시적 unavailable 상태를 추가한다.

#### F9 — Epic 2는 스토리 간 경계에서 protocol 변경을 회귀 테스트에 반영하지 못했다

- **근거:** `TagsRepositoryProtocol`은 `sync_vanished`를 새로 요구한다(`apps/batch/candidate_tags_repository.py:40-45`). 반면 scheduler 공통 fake는 `upsert_tags` 관련 배선만 제공하고 새 메서드가 없다(`tests/batch/test_scheduler.py:69-90`). 이 조합이 F1의 7개 실패를 만들었다.
- **영향:** 개별 Story 2.8 테스트는 통과해도 Story 1.7 scheduler 경계에서 통합 회귀가 발생한다.
- **처리:** **fix now** — shared fake/protocol contract test를 만들고, 다음 story에서 공통 의존성 변경 시 전체 batch suite를 완료 gate로 둔다.

#### F10 — 골든 fixture 자체는 내부적으로 강하지만 운영 gate로 오인될 여지가 있다

- **근거:** 현재 골든 테스트는 backtest suite에 포함돼 Jaccard A/B/C 1.0을 출력하도록 설계됐다(`spec-2-4-골든-픽스처-회귀-테스트.md:99-105, 150-155`). 동시에 fixture가 yfinance 파생 데이터만 반영한다는 deferred high 항목이 있다(`spec-2-4-골든-픽스처-회귀-테스트.md:14-19`).
- **영향:** CI 녹색이 운영 LS 데이터와의 동등성까지 보장한다는 잘못된 완료 신호가 될 수 있다.
- **처리:** **defer** — CI 출력과 배포 gate 문구에 “backtest fixture only”를 명시하고 LS 동등성 검증을 별도 필수 체크로 연결한다.

### Review lens 결과

- **Adversarial:** F1, F3, F4, F5, F6, F8, F10을 확인했다. 공통 경계(protocol·DB·보호 라우트)에서 단일 스토리 테스트가 보지 못하는 위험이 집중됐다.
- **Edge-case-hunter:** `sync_vanished` 실패, partial/failed candidates, 직전 attempt 부재, 완전/부분 소멸은 SQL fixture가 다룬다. 다만 F1의 공통 fake 미지원과 F8의 RPC 오류 표시 비대칭은 남는다.
- **Verification-gap:** F1, F2, F5, F6을 확인했다. 웹 71개 순수 테스트는 통과했지만 보호 라우트와 운영 DB 경계까지는 증명하지 않는다.

## Behavior verification

- `uv run --with pandas --with numpy --with pyarrow --with pytest pytest backtest/tests tests/batch tests/domain -q` — **234 passed, 7 failed**. 실패는 모두 scheduler가 새 `sync_vanished` protocol을 충족하지 않는 fake를 통해 `partial`을 반환한 경로다.
- `npm run test` — **71 passed**.
- `npm run typecheck` — 통과.
- `npm run build` — Next production build 통과. `/`, `/strategies/[strategy]`, `/tracking` 및 API route가 생성됐다.
- `git diff --check` — 통과.
- SQL fixture — 파일과 CI 목록은 확인했으나, 운영 Supabase/로컬 Postgres 실행 결과는 도구 부재로 확인하지 못했다.
- Playwright MCP — 현재 세션에서 사용할 수 있는 MCP 도구가 없어 인증 후 카드·소멸·장중 라벨의 실제 렌더링은 실행하지 못했다.

## Previous-retro follow-through

Epic 1 회고의 7개 action item은 `sprint-status.yaml:106-151`에서 모두 `open`이다. 아래는 현재 증거가 제안하는 상태이며, 사용자 확인 없이 sprint status 상태는 변경하지 않았다.

1. `epic-1-retro-item-1-루트-typescript-게이트를-웹-tsconfig와-일치시키고-ci에` — `npm run typecheck` 및 workflow `.github/workflows/test.yml:50-57` 통과 증거가 있어 **done 전환 제안**.
2. `epic-1-retro-item-2-운영-supabase에-202609012200-202609021100-m` — 현재 환경에서 운영 DB 적용 증거를 찾지 못함. **no evidence found**.
3. `epic-1-retro-item-3-logical_runs-runs-rls-정책을-approved-view` — migration 파일 존재만으로 운영 적용을 증명할 수 없어 **no evidence found**.
4. `epic-1-retro-item-4-pretendard-제공-방식을-csp와-일치시킨다` — `apps/web/next.config.ts:61-62`가 `cdn.jsdelivr.net`을 style/font source에 허용한다. **done 전환 제안**; 실제 브라우저 font 적용은 미검증.
5. `epic-1-retro-item-5-clean-db-n-n-1-generated-read-model-type` — workflow `.github/workflows/test.yml:65-110`에는 migration/fixture job은 있으나 N/N-1 및 generated read-model type diff 단계가 보이지 않는다. **no evidence found**.
6. `epic-1-retro-item-6-운영-배포-전-인증된-수동-dispatch-및-csp를-다루는-playw` — 인증 fixture 부재가 Epic 2 UI spec에도 계속 기록돼 있다(`spec-2-7:13-22`, `spec-2-8:13-25`). **no evidence found**.
7. `epic-1-retro-item-7-스토리-key를-커밋-제목과-build-review-evidence에-표` — Epic 2 커밋 제목은 `story 2-1`~`story 2-8`을 포함하고 `git_evidence.py`가 해당 8개에 귀속했다. **done 전환 제안**.

## Action items

1. **공통 scheduler fake와 batch regression을 `sync_vanished` protocol에 맞게 수정하고 234+ 테스트 전체를 녹색으로 만든다.** Owner: 개발. Proposed remediation; F1/F9.
2. **운영 Supabase에 Epic 2 migration을 적용하고 SQL fixture·RLS·function grants를 실제 프로젝트에서 실행해 결과를 기록한다.** Owner: 운영/개발. Proposed remediation; F6.
3. **LS `sujung=Y`와 backtest raw fixture의 adjustment equivalence 검증을 Epic 3 진입 gate로 추가한다.** Owner: 전략/개발. Proposed process and verification work; F2/F10.
4. **장시간 tags stage에 heartbeat/lease 연장과 저장 실패 격리 정책을 설계한다.** Owner: 배치/개발. Proposed remediation; F3/F4.
5. **인증 세션을 포함한 Playwright MCP smoke에서 카드·부분/완전 소멸·모집단 이탈·수집 실패·장중 라벨을 실제 렌더 결과로 검증한다.** Owner: QA/프론트엔드. Proposed verification work; F5/F8.

## Action item follow-through (09-09-2026)

### Item 3 — LS adjustment equivalence gate (epic-2-retro-item-10): done, **gate FAIL**

- **산출물:** `tools/verify_adjustment_equivalence.py`(LS `t8410 sujung=Y` 실측 → `_reference_signals` A/B/C/D/E/F 재계산 → 전략별 Jaccard gate 0.9 + 종목별 종가 괴리 통계). 실행 시 LS 조회 결과를 `_bmad-output/implementation-artifacts/ls-ohlcv-cache-2025-01-01-2026-08-26.json.gz`에 캐시해 재현한다.
- **실측 결과(2026-08-26 기준, 98종목, 창 2026-01-01~):** A=0.9167, B=0.8750, C=0.9167, D=0.6667, E=1.0000, F=0.7143. **B/D/F 미달 → 게이트 FAIL.** evidence: `_bmad-output/implementation-artifacts/adjustment-equivalence-2026-08-26.json`.
- **원인 분류:** 98종목 중 53종목이 yfinance 대비 LS 수정주가 비율 CV>1%. 무점프 상수 체계 45종(배당조정 방식 차이, ratio 1.02~1.05), **점프 19종은 액면분할·무상증자 불일치**(091810 전 구간 4.78배=분할 미반영, 000070 2025-11-24 무상증자, 009160 0.72배, 006980 2025-12-29 등). 불일치 종목이 B(스토캐스틱)·D(3파)·F(각도) 신호를 뒤집었다.
- **결정:** 사용자 승인에 따라 **게이트 FAIL을 기록하고 item 10 종료**. 골든 gate는 README AD-5에 따라 **'미검증 상태' 유지**(Jaccard 통과는 여전히 배포 승인 근거로 불신). fixture를 LS sujung 기준으로 재생성하고 이 gate를 재검증하는 작업은 **backlog로 남긴다**.

### Item 4 — 장시간 tags stage heartbeat/lease 연장 + 저장 실패 격리 (epic-2-retro-item-11): done

- **산출물 (설계 + 구현):** `_bmad-output/implementation-artifacts/design-epic-2-item-11-heartbeat-저장-격리.md` — F3(lease 만료)와 F4(전량 폐기)의 해법을 설계 문서로 고정.
- **구현:** `apps/batch/heartbeat.py` 신규(`HeartbeatPolicy`/`LeaseHeartbeat`/`HeartbeatExhausted`), `run_tags_stage` 후보별 heartbeat + 후보 단위 저장 격리(`TAGS_PERSIST_PARTIAL`, 빈 태그 upsert 미호출, 부분 저장 시 `sync_vanished` 생략), `ohlcv_cache.py` refresh 두 함수 heartbeat, `scheduler.py`가 candidate stage의 fence/lease token으로 `LeaseHeartbeat` 배선. SQL/RPC 변경 없음 — 기존 `heartbeat_attempt`(item-9에서 운영 적용 완료)를 그대로 사용.
- **회귀:** `uv run --with pandas --with numpy --with pyarrow --with pytest pytest` 전체 **518개 green**(2026-09-09). 신규 테스트: `tests/batch/test_heartbeat.py`, tags_stage 격리·heartbeat, ohlcv refresh heartbeat, scheduler heartbeat 배선.
- **종료 결정:** 기존 동작 대비 한 곳만 의도적으로 변경함 — 태깅 결과 0건일 때 upsert를 호출하지 않음(기존은 빈 리스트로 호출). 저장할 것이 없으면 호출할 이유가 없으므로 정당하다고 판단했으며 관련 scheduler 테스트를 새 격리 정책 기준으로 갱신했다.

## Acceptance verdict

**rejected** — 수용 기준은 `epics.md`에 선언돼 있다(`_bmad-output/planning-artifacts/epics.md:550-672`). 8개 스토리는 모두 완료 처리됐지만, 현재 공통 Python 회귀망이 7개 실패했고(F1/F9), 운영 Supabase migration/RPC/fixture 실행 증거가 없으며(F6), LS 운영 데이터와 골든 fixture의 조정 방식 동등성도 아직 deferred다(F2/F10). 웹 단위 테스트와 build 통과만으로는 이 epic의 배치·DB·보호 라우트 계약을 운영 완료로 수용할 수 없다.

## Open questions

- F1은 `sync_vanished` 실패를 실제 partial로 취급하는 설계를 유지할지, scheduler fake를 갱신하는 것만으로 충분한지 결정이 필요하다.
- 운영 Supabase에서 migration 적용 전후의 RLS·grant·fixture 실행을 어느 change window에서 수행할지 결정이 필요하다.
- LS adjustment equivalence를 Epic 2의 재수용 조건으로 backfill할지, Epic 3 진입 gate로 둘지 결정이 필요하다.
- disappeared RPC 실패를 기존 카드와 동일하게 사용자에게 노출할지 결정이 필요하다.

## Assumptions

대화형 실행이며 epic 2를 사용자가 명시했으므로 headless 가정은 없다. 사용자가 확인하지 않은 이전 action item 상태 전환은 문서에 제안만 기록했고 sprint status에는 적용하지 않았다.
