# Editorial Prose Review — wave-double V1 Architecture Spine

이 문서는 인간 구현자와 리뷰어가 wave-double의 불변 계약을 정확히 읽고, 서로 다른 구현 단위에 같은 규칙을 적용하도록 돕기 위해 존재한다.

- **선행 구조 모델:** Explanation (Conceptual). `review-structure-2026-09-01.md`의 findings를 입력으로 사용했다.
- **보존할 문체:** 짧고 단정적인 규범형 종결(`~한다`), `Binds / Prevents / Rule`의 계약 형식, AD ID, 스키마·API·상태값을 나타내는 영문 기술 용어와 backtick 표기, 핵심 불변식의 제한적 bold 강조.
- **적용 규칙:** 구조 리뷰가 CUT한 `해결된 이전 Deferred 항목`은 검토하지 않았다. MERGE 대상은 AD-5, AD-6, AD-14 및 새 `Canonical outcome & provenance contract`의 생존 위치에 수정안을 붙였다. 코드 블록·frontmatter·표/목록의 markup 자체는 건드리지 않았다.

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | §AD-7 Rule — “브라우저에는 Supabase publishable key만 허용하고 RLS로 승인된 view의 SELECT만 연다. Supabase secret, LS token, GitHub token은 Actions 또는 server-only 환경에만 둔다. `/api/runs/dispatch`는 Supabase JWKS 기반 signature/issuer/audience/expiry와 allowlisted `sub`를 검증하고 **CSRF 보호(이중 제출 쿠키 패턴: `csrf_token` 쿠키 + `x-csrf-token` 헤더 검증, SameSite=Strict)**, 사용자별 rate limit, idempotency key를 통과한 요청만 받는다. Actions write만 가진 fine-grained GitHub token으로 고정 workflow/ref를 호출하며 owner/repo/workflow/ref는 사용자 입력을 받지 않는다. 같은 idempotency key는 기존 dispatch 결과를 반환한다.” | “브라우저에는 Supabase publishable key만 허용하고, RLS로 승인된 view의 `SELECT`만 연다. Supabase secret, LS token, GitHub token은 Actions 또는 server-only 환경에만 둔다. `/api/runs/dispatch`는 Supabase JWKS로 signature, issuer, audience, expiry와 allowlist의 `sub`를 검증한다. 또한 CSRF 보호, 사용자별 rate limit, idempotency key 검사를 모두 통과한 요청만 받는다. 서버는 Actions 쓰기 권한만 가진 fine-grained GitHub token으로 고정된 workflow와 ref를 호출한다. owner, repo, workflow, ref는 사용자 입력으로 받지 않는다. 같은 idempotency key에는 기존 dispatch 결과를 반환한다.” | 인증 검증, 요청 보호, GitHub 호출 제한을 별도 문장으로 분리했다. 한 문장 안의 slash 열거를 병렬 명사로 풀고 주어를 명확히 했다. 괄호 안의 CSRF 구현 세부는 원문대로 유지한다. |
| prose | §AD-3 Rule — “orchestrator만 위 테이블을 변경하며, GitHub Actions `concurrency: {group: logical_run_key, cancel-in-progress: true}`와 DB advisory lock으로 같은 논리 키를 직렬화한다. **attempt 시작 시 DB sequence(`fence_token` BIGINT GENERATED ALWAYS AS IDENTITY)로 단조 증가 토큰을 발급하고, 모든 stage write/publish RPC는 현재 `fence_token`과 `canonical_attempt_run_id` 자격을 검사한다.** advisory lock만을 stale-write 방지 수단으로 사용하지 않는다. 각 stage writer는 자기 테이블과 stage result만 idempotent upsert한다. 상태 전이는 expected status를 받는 전용 RPC/CAS만 허용하고 JSON read-modify-write 같은 임의 mutation을 금지한다.” | “orchestrator만 위 테이블을 변경한다. 같은 논리 키는 GitHub Actions `concurrency: {group: logical_run_key, cancel-in-progress: true}`와 DB advisory lock으로 직렬화한다. **각 attempt를 시작할 때 DB sequence(`fence_token` BIGINT GENERATED ALWAYS AS IDENTITY)로 단조 증가 토큰을 발급한다. 모든 stage write/publish RPC는 현재 `fence_token`과 `canonical_attempt_run_id` 자격을 검사한다.** advisory lock만으로 stale write를 방지해서는 안 된다. 각 stage writer는 자신이 소유한 테이블과 stage result만 idempotent upsert한다. 상태 전이는 expected status를 받는 전용 RPC/CAS로만 수행하며, JSON read-modify-write 같은 임의 mutation은 금지한다.” | 긴 계약을 소유권, 직렬화, fencing, writer, 상태 전이 순으로 분리했다. “자기”의 대상을 “자신이 소유한”으로 명확히 하고 금지 문장의 병렬성을 맞췄다. |
| prose | §Canonical outcome & provenance contract의 생존 위치(기존 AD-15 Rule) — “publication transaction이 `logical_runs.canonical_attempt_run_id`를 단일 행으로 선택한다. **`canonical_attempt_run_id`는 반드시 `status='success'`인 attempt만 선택한다.** `fence_token` 순서는 success attempt 사이에서만 적용된다. partial/failed attempt가 max `fence_token`을 가져도 canonical이 될 수 없다. outcome 생성 함수는 후보가 해당 `canonical_attempt_run_id`에 속하고 그 run이 `success` 출판 상태인지 FK/constraint로 검증한다. source가 여러 fallback 결과의 합집합일 수 있다면 canonical candidate 선택/기여-source 연결 shape을 별도 테이블(`candidate_source_contrib`)로 정의하고, outcome projection의 단일 `entry_candidate_id`만으로 source별 slice를 만들 수 있다는 가정을 금지한다.” | “publication transaction은 `logical_runs.canonical_attempt_run_id`에 하나의 attempt를 선택한다. **선택 대상은 `status='success'`인 attempt로 한정한다.** `fence_token` 순서는 success attempt끼리만 비교한다. partial 또는 failed attempt는 가장 큰 `fence_token`을 가져도 canonical이 될 수 없다. outcome 생성 함수는 후보가 canonical attempt에 속하는지, 해당 run이 출판된 `success` 상태인지 FK 또는 constraint로 검증한다. 여러 fallback 결과를 합친 source는 `candidate_source_contrib`에 표현한다. source별 slice를 outcome projection의 단일 `entry_candidate_id`에서 추론하지 않는다.” | MERGE 뒤의 공통 계약 위치에 적용한다. 같은 식별자의 연속 반복을 줄이고, 선택 조건·순서·검증·source 표현을 짧은 문장으로 나눴다. |
| prose | §AD-17의 생존 spine 계약 — “V1 배포 전 **provider acceptance contract**로 다음을 확정한다: (1) RPO ≤ 1시간, RTO ≤ 4시간, (2) Supabase PITR 활성화 필수(또는 동등한 self-managed backup 검증), (3) migration 전 자동 restore point 생성, (4) 정기 restore drill(분기 1회) 및 복구 책임자/절차 문서화, (5) 복구 후 `current_complete_run_id` 포인터 재설정 및 GitHub schedule 재개 순서 명시.” | “V1 배포 전에 **provider acceptance contract**를 확정한다. 이 계약은 다음을 포함한다: (1) RPO ≤ 1시간과 RTO ≤ 4시간, (2) Supabase PITR 활성화 또는 동등한 self-managed backup 검증, (3) migration 전 자동 restore point 생성, (4) 분기 1회 restore drill과 복구 책임자·절차의 문서화, (5) 복구 후 `current_complete_run_id` 포인터 재설정과 GitHub schedule 재개 순서.” | 구조 리뷰에서 runbook으로 이동하지 않고 spine에 남는 acceptance contract만 교정했다. 서두의 긴 목적어를 독립 문장으로 만들고 번호 항목의 문법 형식을 맞췄다. |
| prose | §AD-18의 생존 spine 계약 — “canonical request hash(전체 payload SHA-256)를 함께 저장해 **같은 key+같은 hash만 replay(기존 `github_dispatch_id` 반환), 같은 key+다른 hash는 `409 Conflict`로 거부**한다.” | “전체 payload를 SHA-256으로 계산한 canonical request hash를 함께 저장한다. **같은 key와 같은 hash의 요청에는 기존 `github_dispatch_id`를 반환하고, 같은 key에 다른 hash가 들어오면 `409 Conflict`로 거부한다.**” | MOVE 뒤에도 남는 idempotency 계약에 적용한다. 명사구에 동작이 압축된 원문을 두 개의 완전한 조건문으로 풀었다. |
| prose | §AD-5 Rule의 생존 위치 — “기존 함수의 broad exception 또는 비유한 입력을 무신호로 바꾸지 말고 구조화된 stage error로 승격한다.” | “기존 함수에서 broad exception이 발생하거나 입력값이 유한하지 않으면, 이를 무신호로 처리하지 않고 구조화된 stage error로 승격한다.” | MERGE 시 AD-5에 남길 문장이다. 서로 다른 문법 성분인 exception과 입력을 같은 목적어로 묶은 표현을 조건절로 바로잡았다. “비유한 입력”을 “유한하지 않은 입력값”으로 풀어 인간 독자의 첫 읽기 이해를 높였다. |
| prose | §AD-6 Dynamic Rate Limit — “토큰 버킷은 `Retry-After` 헤더(초) 또는 `X-RateLimit-Reset` 타임스탬프 파싱해 동적 재충전 간격 조정” | “토큰 버킷은 `Retry-After` 헤더의 초 값 또는 `X-RateLimit-Reset` 타임스탬프를 파싱해 재충전 간격을 동적으로 조정한다.” | MERGE 시 AD-6에 남길 문장이다. 빠진 목적격 조사와 서술어를 보완하고 수식 관계를 명확히 했다. |
| prose | §AD-3 Cross-attempt — “이렇게 하면 stale worker가 이전 attempt의 stage row를 쓰지 못하도록 하고, read model join 시 단일 attempt의 stage만 보이게 한다.” | “이 처리로 stale worker가 이전 attempt의 stage row를 쓰지 못하게 하고, read model을 join할 때는 단일 attempt의 stage만 노출한다.” | “쓰지 못하도록 하고”의 어색한 연결을 고치고, “이렇게”가 가리키는 대상을 “이 처리”로 좁혔다. 동사 `보이다` 대신 시스템 주체가 드러나는 `노출한다`를 사용했다. |
| prose | §AD-17 restore runbook 이동 위치 — “restore drill 시 staging DB의 `restore_epoch`를 프로덕션과 동일하게 설정해 double-count 방지.” | “restore drill에서는 staging DB의 `restore_epoch`를 프로덕션과 동일하게 설정해 중복 집계를 방지한다.” | MOVE될 runbook 문장에 적용한다. 조사를 보완하고 명사형 파편을 완전한 문장으로 바꿨다. `double-count`는 이미 쓰는 한국어 개념인 “중복 집계”로 통일했다. |
| prose | §Canonical outcome & provenance contract의 생존 위치(기존 AD-19) — “delete-or-mark 선택지를 없애고 강제 삭제→단일행 재생성으로 고정한다.” | “delete-or-mark 선택지를 없애고, 삭제한 뒤 단일 행으로 재생성하는 흐름으로 고정한다.” | MERGE 뒤의 공통 계약 위치에 적용한다. 기호로 압축된 동작 순서를 문장으로 풀고 `단일 행` 띄어쓰기를 바로잡았다. |
| prose | §AD-18 dispatch runbook 이동 위치 — “reconciler는 `pg_notify('dispatch_reconcile', ...)` trigger(INSERT 시 발생) 또는 Supabase Database Webhooks로 즉시 실행되어 `pending` 초과 TTL을 `unknown`으로 올리고 GitHub API로 실제 dispatch 상태를 조회해 동기화한다.” | “reconciler는 INSERT 때 발생하는 `pg_notify('dispatch_reconcile', ...)` trigger 또는 Supabase Database Webhooks로 즉시 실행한다. TTL을 초과한 `pending` 상태는 `unknown`으로 전환하고, GitHub API에서 실제 dispatch 상태를 조회해 동기화한다.” | MOVE될 runbook 문장에 적용한다. 실행 계기, timeout 전이, 외부 조회를 분리하고 “`pending` 초과 TTL”의 역전된 수식 관계를 바로잡았다. |
| prose | 문서 전반 — “`run_id`는 시도마다 새 UUID”, “attempt 시작 시”, “Cross-attempt stage 중복 방지”, “재시도는 `attempt_no`만 증가” | “첫 등장: ‘각 실행 시도(attempt)에는 새 UUID `run_id`를 부여한다.’ 이후: ‘attempt를 시작할 때’, ‘attempt 간 stage 중복 방지’, ‘같은 논리 실행의 재시도는 `attempt_no`만 증가시킨다.’” | 같은 핵심 개념을 `시도`, `attempt`, `Cross-attempt`, `재시도`로 바꾸어 부른다. 첫 등장에 한국어와 schema 용어를 연결한 뒤 backtick의 `attempt`로 통일한다. 동일 문제는 한 행으로 묶었다. |

## Summary

- **Prose 권고:** 12건. 긴 계약 문장 분리 5건, 문법·수식 관계 교정 6건, 핵심 용어 통일 1건.
- **예상 순감소:** 약 **70~110 words**(원문 7,321 words의 약 **1~2%**). 목표는 축약보다 오독 방지다.
- **구조 findings와의 관계:** 구조 리뷰의 900~1,220 words 이동·병합 권고와 별개다. CUT 구절에는 prose 수정을 제안하지 않았고, MERGE 구절의 수정은 생존 계약 위치에만 연결했다.
- **보존 사항:** 규범형 목소리, AD 식별자, 기술 용어, 도식과 표, 코드·DDL·frontmatter는 그대로 둔다.
